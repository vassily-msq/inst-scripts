# SET UP ENTIRE JS ENVIRONMENT
# sudo apt install nodejs
# sudo apt install npm


If the above command installs dated npm/node versions, install node as a snap package. 
# sudo snap install node --classic --channel=12
sudo snap install node --classic

# Install components globally
sudo npm install -g live-server
sudo npm install -g react
sudo npm install -g lodash


# Remove node
# sudo apt remove nodejs
# sudo apt remove npm
# sudo apt purge nodejs
# sudo apt purge npm
# sudo snap remove node

# misc
# npm run build --scripts-prepend-node-path